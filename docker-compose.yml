services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pm-backend
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    restart: unless-stopped
